# Resolve peer conflicts from @langchain/community's optional peer
# (@browserbasehq/stagehand wants dotenv@^16 and openai@^4; we use dotenv 16 and openai 6).
# We only use PuppeteerWebBaseLoader from community; stagehand is for Browserbase, not required.
legacy-peer-deps=true
